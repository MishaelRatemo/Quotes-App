import { HighesUpvoteHighlighterDirective } from './highes-upvote-highlighter.directive';

describe('HighesUpvoteHighlighterDirective', () => {
  it('should create an instance', () => {
    const directive = new HighesUpvoteHighlighterDirective();
    expect(directive).toBeTruthy();
  });
});
